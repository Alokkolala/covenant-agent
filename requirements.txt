# Deliberately tiny. No agent framework, no vector DB - see README "What we did not build".
pymupdf>=1.24      # PDF text + tables + coordinates, and fixture generation
pandas>=2.0        # only for .xlsx registries; everything else is stdlib csv/json
